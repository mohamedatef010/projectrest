 🚀 دليل نشر المشروع على Vercel

## المشكلة التي تم حلها
عند رفع الفرونتيند على Vercel، كانت جميع طلبات API تفشل بخطأ 404 لأن Vercel لا يحتوي على proxy مثل Vite في التطوير المحلي.

## ✅ الحل المطبق

### 1. إنشاء نظام API موحد
تم إنشاء ملف `client/src/lib/api.ts` الذي يتعامل تلقائياً مع:
- استخدام URLs نسبية في التطوير المحلي (مع Vite proxy)
- استخدام URL الباك إند الكامل في الإنتاج (Vercel)

### 2. تحديث queryClient
تم تحديث `client/src/lib/queryClient.ts` لاستخدام نظام API الجديد.

## 📋 خطوات النشر على Vercel

### الخطوة 1: رفع الباك إند أولاً
قبل نشر الفرونتيند، يجب أن يكون الباك إند متاحاً على الإنترنت. يمكنك استخدام:
- **Railway**: https://railway.app
- **Render**: https://render.com
- **PythonAnywhere**: https://www.pythonanywhere.com
- **Heroku**: https://heroku.com
- **DigitalOcean**: https://www.digitalocean.com

مثال لرفع الباك إند على Railway:
```bash
# في مجلد backend
railway login
railway init
railway up
```

### الخطوة 2: الحصول على URL الباك إند
بعد رفع الباك إند، ستحصل على URL مثل:
```
https://your-app-name.railway.app
```

### الخطوة 3: إعداد متغيرات البيئة في Vercel

1. اذهب إلى لوحة تحكم Vercel: https://vercel.com/dashboard
2. اختر مشروعك
3. اذهب إلى **Settings** → **Environment Variables**
4. أضف المتغيرات التالية:

```env
VITE_API_URL=https://your-backend-url.com/api
VITE_APP_NAME=Istanbul Restaurant
VITE_SOCKET_URL=wss://your-backend-url.com
```

⚠️ **مهم جداً**: استبدل `your-backend-url.com` بعنوان الباك إند الفعلي الخاص بك!

### الخطوة 4: إعادة النشر
بعد إضافة متغيرات البيئة:
1. اذهب إلى **Deployments**
2. اضغط على **Redeploy** للنشر الأخير
3. أو قم بعمل push جديد إلى GitHub

## 🔧 إعدادات إضافية

### تحديث ملف .env.production (اختياري)
يمكنك تحديث ملف `.env.production` بعنوان الباك إند الخاص بك:

```env
VITE_API_URL=https://your-backend-url.com/api
VITE_APP_NAME=Istanbul Restaurant
VITE_SOCKET_URL=wss://your-backend-url.com
```

### إعدادات CORS في الباك إند
تأكد من أن الباك إند يسمح بطلبات من نطاق Vercel الخاص بك:

في `backend/app.py`:
```python
CORS(app, 
     supports_credentials=True,
     origins=[
         "http://localhost:5173",
         "https://your-vercel-app.vercel.app",  # أضف نطاق Vercel
         "https://your-custom-domain.com"       # إذا كان لديك نطاق مخصص
     ],
     allow_headers=["Content-Type", "Authorization", "Accept"],
     methods=["GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH"]
)
```

## 🧪 اختبار النشر

بعد النشر، افتح Console في المتصفح (F12) وتحقق من:

1. **طلبات API تذهب للعنوان الصحيح**:
   ```
   ✅ https://your-backend-url.com/api/categories
   ❌ /api/categories (خطأ - URL نسبي)
   ```

2. **لا توجد أخطاء CORS**:
   ```
   ✅ Access-Control-Allow-Origin header موجود
   ```

3. **الاستجابات ناجحة**:
   ```
   ✅ Status: 200 OK
   ❌ Status: 404 Not Found
   ```

## 📝 ملاحظات مهمة

### 1. متغيرات البيئة في Vercel
- يجب أن تبدأ بـ `VITE_` لكي يتم تضمينها في البناء
- يتم قراءتها فقط أثناء البناء (build time)
- أي تغيير يتطلب إعادة نشر

### 2. الفرق بين التطوير والإنتاج
- **التطوير**: يستخدم Vite proxy → URLs نسبية تعمل
- **الإنتاج**: لا يوجد proxy → يجب استخدام URLs كاملة

### 3. أمان الباك إند
- لا تضع معلومات حساسة في متغيرات البيئة في الفرونتيند
- جميع متغيرات `VITE_*` تكون مرئية في الكود المبني
- معلومات قاعدة البيانات يجب أن تبقى في الباك إند فقط

## 🐛 حل المشاكل الشائعة

### المشكلة: 404 Not Found
**السبب**: URL الباك إند غير صحيح أو الباك إند غير متاح
**الحل**: 
1. تحقق من أن الباك إند يعمل
2. تحقق من `VITE_API_URL` في Vercel
3. افتح URL الباك إند في المتصفح للتأكد

### المشكلة: CORS Error
**السبب**: الباك إند لا يسمح بطلبات من نطاق Vercel
**الحل**: أضف نطاق Vercel إلى قائمة `origins` في إعدادات CORS

### المشكلة: Environment Variables لا تعمل
**السبب**: لم يتم إعادة النشر بعد تغيير المتغيرات
**الحل**: اذهب إلى Deployments → Redeploy

### المشكلة: الموقع يعمل محلياً لكن لا يعمل على Vercel
**السبب**: متغيرات البيئة مختلفة أو غير موجودة
**الحل**: 
1. تحقق من Environment Variables في Vercel
2. تأكد من أن جميع المتغيرات المطلوبة موجودة
3. أعد النشر

## 📚 موارد إضافية

- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
- [Vite Environment Variables](https://vitejs.dev/guide/env-and-mode.html)
- [CORS Configuration](https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS)

## ✅ قائمة التحقق النهائية

قبل النشر، تأكد من:

- [ ] الباك إند منشور ويعمل على الإنترنت
- [ ] حصلت على URL الباك إند الكامل
- [ ] أضفت `VITE_API_URL` في Vercel Environment Variables
- [ ] أضفت نطاق Vercel إلى CORS في الباك إند
- [ ] اختبرت الباك إند مباشرة (مثلاً: `https://your-backend.com/api/health`)
- [ ] أعدت نشر المشروع على Vercel بعد إضافة المتغيرات
- [ ] فتحت Console في المتصفح للتحقق من عدم وجود أخطاء

---

**تم إنشاء هذا الدليل في**: 30 يناير 2026
**آخر تحديث**: 30 يناير 2026
